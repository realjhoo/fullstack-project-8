extends layout

block content
  h1 Update Book
  form(method="post" action="")
    p
      label(for="title") Title
      input(name="title" type="text" id="title" value=book.title)
    p
      label(for="author" ) Author
      input(name="author" type="text" id="author" value=book.author)
    p 
      label(for="genre") Genre
      input(name="title" type="text" id="title" value=book.genre)
    p 
      label(for="year") Year
      input(name="title" type="text" id="title" value=book.year)
    p
      input(type="submit" value="Update Book")
  form(action="/", method="post", onsubmit="return confirm('Delete? Really?');")
    p
      a.button(href="/") Cancel
    input(type="submit" value="Delete Book")
    br
    br
    h5 this is update-book.pug